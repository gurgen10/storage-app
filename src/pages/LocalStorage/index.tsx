import { Box, Typography } from '@mui/material'
import './LocalStorage.css'

export const LocalStorage = () => {
    return (<Box>
        <Typography>
            LocalStorage
        </Typography>
    </Box>)
}